# Discord API with Voice Receive Support and DAVE E2EE protocol
py-cord[voice] @ git+https://github.com/Pycord-Development/pycord.git@refs/pull/3159/head

# Environment variables
python-dotenv>=1.0.0

# Voice encryption for Discord
PyNaCl>=1.5.0

# WebSocket client for Moshi server communication
aiohttp>=3.9.0

# Audio resampling (48kHz Discord <-> 24kHz PersonaPlex)
torchaudio

# Numerical operations for PCM audio processing
numpy

# High-performance SenseVoice ASR (Fish Audio)
funasr
modelscope

# Voice Activity Detection (The First Gate)
# WHY: Prevents the 5090 from burning cycles on silence.
silero-vad

# Neural Keyword Spotting (The Ear)
# WHY: Sherpa-ONNX Zipformer provides temporally-aware phoneme spotting.
sherpa-onnx>=1.13.0
sentencepiece

# AI Training & Optimization (The Muscle)
# WHY: Required for BitsAndBytes and model loading optimizations.
accelerate
bitsandbytes>=0.45.0

# Remote API communication
requests
